@model IEnumerable<WebTest.ViewModels.DiseaseTreatmentFactorViewData>
@{
    AjaxOptions options = new AjaxOptions();
    options.HttpMethod = "POST";
    options.OnFailure = "ReportFailure";
    //options.OnBegin = "OnBegin";
    options.OnSuccess = "ActivateTab(3)";
    options.LoadingElementId = "ajax-loader";
    options.LoadingElementDuration = 1000;
    options.UpdateTargetId = "factors";
    options.InsertionMode = InsertionMode.Replace;

}
@*@if (Model != null && Model.Count() > 0)
{*@
    @using (Ajax.BeginForm("SaveReceivedTreatments", "PatientProfile", options))
    {
      
        <div class="form-group">
            <div class="control-label"></div>
            <div>
                <button type="submit" class="btn btn-default">Save</button>
            </div>
        </div>
    }
@*}
else
{
    <p class="text-warning">This page will be populated when Patient profile has been created.</p>
}*@